{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Index.html in Chrome",
            "file": "${workspaceFolder}/Index.html"
        },
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Current File in Chrome",
            "file": "${file}"
        }
    ]
}